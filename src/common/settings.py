"""
⚠️  DEPRECATED MODULE

`common.settings` has been split into:

   • `common.schema`   (Pydantic models)
   • `common.loader`   (load_settings helper)

Update your imports, e.g.:

   from common.loader import load_settings
   from common.schema import Settings
"""

raise ImportError(__doc__)

